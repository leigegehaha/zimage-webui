目前这个项目里，我比较满意的几个点有：

- 本地优先：代码拉下来就能在本地跑，不依赖远程平台
- 不需要 ComfyUI：直接网页交互，不用自己排节点
- 参数够用：宽高、比例、分辨率、步数、种子、引导强度都能调
- 支持任务队列：一个任务在跑的时候，还可以继续发下一个
- 有进度条和耗时统计：不是点一下就纯等黑盒
- 有历史记录：下次打开还能看到之前生成图对应的参数
- 可以预览、下载，也能做基础图片编辑
- 高级设置做成抽屉，不会把主界面弄得太乱

另外界面整体做成了像素风、高饱和度的方向，尽量不让它看起来像那种“功能是齐了，但视觉像后台管理系统”的工具页。
